# tests/

Smoke + correctness tests for the core components. Run with:

    pytest tests/ -v

(or `python -m pytest tests/ -v` if pytest is installed; otherwise each
test_*.py file is runnable directly as a script.)