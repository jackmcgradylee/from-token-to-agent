# W1 sample set: mixed Chinese / English / Code / digits / whitespace.
# Used by experiments/w01/exp-001-char-word-bpe-comparison/runner.py.
#
# Train/eval split is marked by `## eval`. Everything before is the
# training corpus (used to build vocab / BPE merges). Everything after
# is the held-out eval set — chosen to contain items the word tokenizer
# has NOT seen, so it produces <unk>. Char and BPE have zero UNK by
# construction.
#
# Each line is one sentence of samples. Long enough to be representative
# (CJK + ASCII + code), short enough to read in a table.

# English prose
Once upon a time, there was a little girl named Lily.
The quick brown fox jumps over the lazy dog.
Hello world! This is a test.

# Digits and whitespace
1234567890
   leading and trailing whitespace
Repeated words: the the the the the.

# Chinese
你好，世界。
机器学习是从数据中提取模式的人工智能分支。

# Mixed
Mixing 中文 and English: hello, world.
Transformer 是一种基于 self-attention 的序列模型。

# Code (Python)
def greet(name: str) -> str:
    return f"Hello, {name}!"

# Code (C / bracket-heavy)
for (int i = 0; i < n; i++) { sum += a[i] * b[i]; }

## eval
# Held-out samples. The first 3 should produce UNK with the word
# tokenizer because those exact words never appear in the training
# corpus above (rare forenames, rare CJK chars, emojis).

# English OOV — uncommon words
Cassiniophilina's zephyr-laden persiflage bewildered the quorum.
Quixotry and rheumy bibliobibuli vexed the catachresis.

# CJK OOV — uncommon characters (rare surnames / technical terms)
嵚崟嶷魑魅魍魉孀嫠。

# Mixed + emoji
Quantum 🎯 entanglement and 🧬 sequencing herald a new era.

# Code OOV — uncommon operators
auto&& [this]() noexcept -> decltype(auto) { return *this; }