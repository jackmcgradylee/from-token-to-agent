# Architecture diagrams

Hand-drawn architecture diagrams for this codebase. Generated by us, not copied from upstream projects.

Conventions:

- ASCII for inline diagrams (in week notes / ROADMAP)
- SVG / PNG for "hero" diagrams (added here when needed)
- One diagram per file: `<filename>.<ext>` plus a one-paragraph caption

Topics to cover as the course progresses:

- `signal-ladder.svg` — the 7-rung learning-signal ladder
- `tokenizer-pipeline.svg` — char / word / BPE comparison (W1)
- `transformer-block.svg` — pre-norm decoder block with RoPE / RMSNorm / GQA / SwiGLU (W2)
- `training-stack.svg` — tokenizer → dataset → Transformer → optimizer → checkpoint (W3)
- `attention-backends.svg` — reference / pytorch / triton interfaces (W4)
- `serving-loop.svg` — TTFT / TPOT / continuous batching / KV cache (W5)
- `data-pipeline.svg` — raw → parse → filter → dedup → corpus → tokenize (W6)
- `synthetic-factory.svg` — task generator → solver → verifier → judge → training data (W7)
- `post-training-routes.svg` — SFT / DPO / RLVR on same base (W8–W10)
- `agent-loop.svg` — task → model → action → environment → observation → ... → verifier (W11–W12)
- `memory-architecture.svg` — working / episodic / procedural memory (W13)
- `self-judge-loop.svg` — trajectory → self-judge → retry → verifier (W14)
- `eval-matrix.svg` — capability / cost / reliability / safety dimensions (W15)

**None of these exist yet.** Add them as the corresponding week is done.